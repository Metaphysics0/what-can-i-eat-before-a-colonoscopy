generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model foods {
  id       String  @id @default(auto()) @map("_id") @db.ObjectId
  Message  String?
  category String
  number   Int
  subType  String
}
